package Handler;

import com.google.gson.Gson;
import spark.Request;
import spark.Response;
import spark.Route;


//public record RegistrationHandle() implements Route {
//
////    @Override
////    public Object handle(Request request, Response response) {
////        Gson gson = new Gson();
//////        RegistrationRequest request = (RegistrationRequest).gson.fromJson();
//////        try {
//////            //request = gson.fromJson(request.body(), RegisterRequest.class)
//////        }
////
////    //}
////}
